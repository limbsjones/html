
function golfScore(par, strokes) {
  // Only change code below this line
  if (golfScore == 4, 1){
      return "Hole-in-one!";
  } else if (golfScore == 4, 2){
      return "Eagle";
  } else if (golfScore == 3, 2) {
    return "Birdie";
  }
  
  return "Change Me";
  // Only change code above this line
}

// Change these values to test
golfScore(4, 2);
